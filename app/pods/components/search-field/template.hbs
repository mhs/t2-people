{{input value=query placeholder=placeholder spellcheck="false"}}

<ul class="search-field-results">
  {{#each project in results}}
    <li>{{#link-to "project" project.id}}{{project.name}}{{/link-to}}</li>
  {{else}}
    {{#if isSearching}}
      <li>{{progress-spinner visible=true}}</li>
    {{/if}}
  {{/each}}
</ul>
